---
id: DEC-0034
type: invariant
status: validated
scope: project
statement: >-
  Lo store vivo `~/.anon` resta in CHIARO con mode 0600 — le mappe (che contengono il valore reale
  dietro ogni placeholder) e il dizionario privato NON vengono cifrati a riposo — e la copia cifrata
  è il backup: `scripts/anon-home-backup.sh` (tar + AES-256-CBC PBKDF2, 600000 iterazioni),
  VERIFICATO nel giro stesso in cui viene scritto (decifratura + confronto di ogni file con un
  manifest sha256 tenuto dentro l'archivio) e ripristinato solo dopo la stessa verifica, senza mai
  cancellare: il ripristino rinomina l'albero corrente in `<home>.pre-restore-<data>` prima di
  scrivere. Nessuna chiave entra nel percorso di esecuzione: `anon.py` scrive e rilegge una mappa
  senza chiedere un segreto e senza tenere una chiave in memoria, in un file o in una variabile
  d'ambiente. Cifrare a riposo lo store vivo cambierebbe il contratto dell'engine e della guardia:
  è una proposta da rifiutare o da supersedere esplicitamente, non da "migliorare" di nascosto.
reason: >-
  Supersede DEC-0033, il cui NUCLEO resta valido e identico: qui cambia una citazione e una contea,
  non la regola. DEC-0033 nominava per esteso lo script di backup di un repository privato, e
  fissava il numero di test della suite ("18 test"): il nome è materiale che non appartiene a un
  repository pubblico, e il numero è una derivata che il progetto ha già visto marcire (la suite è
  cresciuta). La regola — store in chiaro 0600, cifratura solo sulla copia che esce, verifica nel
  giro stesso, nessuna chiave nel percorso d'esecuzione — non cambia di una virgola, e il motivo per
  cui era stata fissata resta: il motore è deterministico e senza stato di sessione (DEC-0012), una
  passphrase non ha dove stare, e la cifratura a riposo si annullerebbe da sé perché chi legge le
  mappe legge anche la chiave. Un record append-only non si corregge sul posto: si supersede.
evidence:
  - scripts/anon-home-backup.sh
  - tests/test_backup.py
  - Makefile
  - anon.py
  - SECURITY.md
authority: high
mutable: false
verify: auto
verify_command: "python3 tests/test_backup.py"
created: 2026-09-26
validated_at: 2026-09-26
supersedes: DEC-0033
---

# DEC-0034 — Lo store vivo è in chiaro 0600, la copia che esce è cifrata

## Statement

Lo store privato (`~/.anon`: le mappe con i valori reali, il dizionario, gli output redatti) vive in
**chiaro con mode 0600**. La cifratura esiste **solo** sulla copia che lascia la macchina:
`scripts/anon-home-backup.sh backup`, AES-256-CBC con PBKDF2 a 600000 iterazioni, verificata nel giro
stesso in cui viene scritta.

Il ripristino (`… restore`) verifica prima di scrivere — decifra, controlla che ogni membro
dell'archivio sia un file regolare con nome relativo, confronta ogni file con il manifest interno, e
confronta il numero di file col numero di righe del manifest — e **non cancella niente**: sposta
l'albero corrente in `<home>.pre-restore-<data>` e scrive nella destinazione vuota. `--force` è la
condizione per farlo; senza, un `~/.anon` non vuoto è un rifiuto.

## Perché supersede DEC-0033

Il nucleo è lo stesso; cambiano due sole cose, entrambe derivanti dalla forma del record precedente:

- **una citazione**: DEC-0033 nominava per esteso lo script di backup di un repository privato, come
  precedente storico dello stesso errore. Il nome non serve alla regola e non appartiene a un
  repository pubblico: qui il precedente resta raccontato, senza il nome.
- **una contea**: DEC-0033 fissava "18 test" per la suite di verifica. La suite è cresciuta: il numero
  era una derivata destinata a marcire, ed è esattamente la classe di difetto (una prosa che resta
  indietro rispetto al codice) che il progetto rifiuta. La verifica si enuncia ora come "la suite
  passa", non come un numero.

La regola non cambia. Se cambiasse, questo record non basterebbe: servirebbe una decisione nuova, con
la sua evidenza.

## Cosa NON vieta

- **Un archivio cifrato dello store**: è il punto di questa decisione, non un'eccezione.
- **`openssl`, `tar`, `shasum`**: sono strumenti del backup, che gira a mano sulla macchina. Nulla di
  tutto questo entra in `anon.py`/`deanon.py` (DEC-0012 resta intatto: il motore è stdlib-only).
- **Il backup globale della macchina**: quello copre anche transcript e credenziali, vive fuori da
  questo repository e resta dov'è. Questo repo possiede il backup del **proprio** store, perché è
  l'unico che anche un utente del prodotto ha.

## Perché non cifrare a riposo

Una mappa è il ponte fra un placeholder e il valore reale: se la cifrassi, ogni operazione dovrebbe
procurarsi la chiave. Le sole due possibilità sono chiederla a ogni run (e allora `deanon.py` lanciato
da solo non legge più nulla, la guardia non può più bonificare, il container non può montare lo store)
oppure tenerla dove il processo la trova — cioè accanto alle mappe, il che annulla la cifratura a
riposo contro l'unico avversario che conta (chi ha già accesso a quel filesystem).

Il modello di minaccia che la cifratura a riposo copre è **il supporto che esce dalla macchina**: la
cartella copiata sul NAS, il disco perso, il backup su una chiavetta. Ed è esattamente lo scenario in
cui entra in gioco l'archivio cifrato.

## Verifica

`verify: auto` esegue `python3 tests/test_backup.py` (nessun Docker, nessuna rete): giro completo con
confronto byte-per-byte e mode, esclusione del modello e delle cache, il valore reale assente dal
cifrato, e una famiglia di rifiuti — passphrase sbagliata (che non deve creare la destinazione),
payload che contraddice il proprio manifest, file non elencato dal manifest, nome `../`, nome
assoluto, membro symlink, archivio senza manifest, passphrase vuota, destinazione esistente, target
non vuoto senza `--force`. Ogni protezione è provata per iniezione del difetto: la suite passa, e il
suo numero cresce col corpus senza che questo record lo debba fissare.

Verificato anche sul dato reale il 2026-09-25: 517 file dello store vivo, backup e ripristino in una
directory di lavoro, insiemi di file e hash identici, nessun contenuto letto in chiaro dall'agente.
