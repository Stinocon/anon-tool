# catalogs/vendors.txt — hardware and software vendors of the IT sector (starter list).
#
# WHAT IT IS
#   The manufacturers whose names appear in an infrastructure report: servers, network gear,
#   storage, printers, endpoints, security and infrastructure software. ONE LINE PER COMPANY, not
#   per product — `Fortinet` is here, `FortiGate`/`NSa 2700` are not. The selection is the names
#   that actually show up in Italian client documents: the international manufacturers plus the
#   main Italian software houses (`Zucchetti`, `TeamSystem`, `Dedagroup`, `GPI`, `Almaviva`,
#   `Maggioli`) and the industrial-networking vendors an OT report names (`Moxa`, `Advantech`,
#   `Belden`, `Riello`).
#
# WHY IT IS OPT-IN
#   The vendor mix is part of what makes a document attributable: "firewall SonicWall, backup Veeam,
#   virtualizzazione VMware" describes the client's stack. It says nothing on its own, so the list
#   is NOT applied unless you select it (`--catalogs vendors`, or the checkbox in the UI).
#
# WHY THERE ARE TWO BLOCKS, AND WHY THE FIRST ONE MATTERS
#   A vendor name that is ALSO an ordinary word must not be matched case-insensitively, or the
#   redaction eats the text around it. Block 1 is `@match case-sensitive` for exactly those names:
#   `Dell`/`dell'aria`, `Canon`/`canon`, `Axis`/`axis`, `Acer` (the maple genus), `Apple`/`apple`,
#   `Juniper`/`juniper`, `Brother`/`brother`, `Xerox`/`xerox`, `Adobe` (the building material),
#   `Google`/`to google`, `Oracle`/`oracle`, `Amazon` (the river), `Slack`/`slack`, `Elastic`,
#   `Snowflake`, `Confluence`, `Tenable`, `Barracuda` (the fish), `Cisco` (the fish), `Siemens` (the
#   SI unit), `Okta` (the cloud-cover unit), `Gigabyte` (the unit), `HP` (horsepower), `SAP` (tree
#   sap), `Arista` (an Italian roast, and the awn of wheat), `Moxa` (burnt in moxibustion), `Sage`
#   (the herb), `Zebra` (the animal), `Ruckus` ("a ruckus"), `Oki`, `Intel`/"threat intel", and the
#   multi-word names whose every word is ordinary (`Red Hat`, `Trend Micro`, `Western Digital`,
#   `Check Point`, `New Relic`, `Open Text`).
#   Block 2 is `@match insensitive`, for names whose lowercase is NOT a word, so a lowercase
#   spelling in a filename, a hostname or a spreadsheet matches too. That is why the short all-caps
#   brands live there — `IBM`, `AMD`, `HPE`, `APC`, `QNAP`, `SUSE`, `ESET`, `ASUS`: none of them is
#   a word, and `un server ibm` in a spreadsheet must be redacted as much as `IBM` in prose.
#   A camel-case compound (`SolarWinds`, `Bitdefender`, `OpenText`) is judged by its OWN surface,
#   not by a spaced form that does not exist in the entry: the separator rule only applies BETWEEN
#   the words of a multi-word entry, so `SolarWinds` does NOT match "solar winds" — which is why it
#   can stay in block 2 — while `Open Text`, being two tokens, DOES match "open text", and is
#   therefore in block 1.
#   Consequence to know: the map keeps the EXACT spelling it found, so a document that writes both
#   `SonicWall` and `sonicwall` gets two placeholders for the same company (deanon restores both).
#   THE LINE IS MEASURABLE, AND IT IS NOW A GATE: a name whose lowercase appears in a word list
#   belongs in block 1. `VendorsCatalogTest` enforces both directions — every block-1 name must
#   record the ordinary word it collides with, and no block-2 name may be a word — and it sweeps
#   block 2 against the OS dictionary as well. The OS list alone is NOT sufficient (it misses
#   `gigabyte`, `google`, `veritas`, `siemens`, `okta`, `hp`, `intel`, `xerox`, `arista`), which is
#   why the recorded word is the primary evidence and the sweep is the net. The gate is what
#   `docs/OPEN-ISSUES.md` #37 asked for, and what the fifteen wrong-block names of the first two
#   reviews cost.
#
# WHAT IS NOT COVERED (declared, not forgotten)
#   * PRODUCT AND MODEL NAMES (`FortiGate`, `PowerEdge`, `Catalyst`, `BIG-IP`, `vSphere`, `NSa`).
#     Product families are a much larger and faster-drifting list, and they usually show up as
#     hostnames, which the HOST rule already covers. Add the ones you meet to `~/.anon/entities.txt`.
#   * THE LOWERCASE SPELLING OF A BLOCK-1 NAME (`dell`, `hp`, `canon`): that is the whole point of
#     block 1.
#   * A DECLARED FALSE POSITIVE: a sentence that opens with the elided Italian article and the
#     company name — "Dell'azienda risulta..." — IS redacted, because an apostrophe is not a word
#     character and no entry can express a negative lookahead. The format has no per-entry
#     exclusion: if it bites, keep the list off for that document or drop `Dell` here.
#   * Vendors that are ordinary words with no unambiguous form are deliberately ABSENT, not
#     forgotten: `LG`, `F5` (the bare token IS the refresh key — the company is here as
#     `F5 Networks`), `MSI` (the Windows installer extension), `AVG`, `Sharp`, `Crucial`,
#     `Canonical`, `Meta` (also an Italian noun), `Docker`/`Kubernetes`/`Ubuntu`/`MongoDB`
#     (the technology, not the firm — and `mongodb` IS how a code file names the database; the
#     clean-corpus sweep is what showed it, one line of `delegate.ts`). The same words, and
#     `Zoom`, live in `catalogs/products.txt` instead: there they are the product or the platform,
#     and a name belongs to ONE of the two catalogs.
#   * This list does not make a document anonymous on its own — it removes one fingerprint among
#     several.
#
# MEASURED COST
#   Selecting it adds 164 entries to the scan. The Pi guard's cap is sized for a few hundred
#   (`scripts/bench-check.py`): measured at this size, `--entities 164 --mb 5` gives **1.82 MB/s**
#   (1.42 MB/s on a 1.25 MB corpus), so the 12 MB cap needs well under half of its 20 s budget;
#   at 1 000 entries the same tool reports 0.93 MB/s and the margin starts to go —
#   `docs/OPEN-ISSUES.md` #26 tracks that.
#   A clean-corpus sweep is what decided the CONTENT, and it is not a score for this file — read it
#   with that in mind:
#     python3 scripts/fp-sweep.py --entities catalogs/vendors.txt --show FORNITORE
#   The number it reports is dominated by documents that NAME vendors — this project's own docs and
#   this header included — so it is a list of things to look at, not a false-positive rate. Outside
#   those, what is left is the corpus talking about its own subject: `MikroTik` in the operator's
#   notes, `microsoft` inside the OOXML namespace `urn:schemas-microsoft-com:vml`, `Slack` in a
#   regex description. (A word list is the better tool for the block question; see below.) What the
#   sweep removed is the entries whose word is the TECHNOLOGY rather than the firm: on the first
#   candidate (measured before this file shipped, 54 clean source/doc files), `Docker` alone
#   accounted for 37 of the 49 hits — three quarters of the noise from one word — and
#   `Kubernetes`/`Ubuntu` belong to the same class. The container path never rewrites an ATTRIBUTE,
#   so the OOXML namespace cannot be corrupted
#   (`VendorsCatalogTest::test_xml_namespaces_survive_the_redaction`).
#
# HOW TO EXTEND IT
#   Append the company, one per line, under the block that fits its surface — under `@match
#   case-sensitive` whenever the name is also an ordinary word or unit. Then run the suite: the gate
#   FAILS a case-sensitive name that does not record its word in `VendorsCatalogTest.
#   WORD_COLLISION`, and FAILS a block-2 name that a word list recognises. Never invent entries and
#   never generate them: a list that LOOKS complete but is not creates a false sense of coverage,
#   and the header count above is bound to the file by `scripts/check-doc-numbers.py`.

@type FORNITORE

# --- block 1: case-sensitive (the name is also an ordinary word) ---
@match case-sensitive
Acer
Adobe
Amazon
Apple
Arista
Avast
Axis
Barracuda
Brother
Canon
Check Point
Cisco
Confluence
Dell
Elastic
Gigabyte
Google
HP
Intel
Juniper
Moxa
New Relic
Oki
Okta
Open Text
Oracle
Red Hat
Ruckus
Sage
SAP
Siemens
Slack
Snowflake
Tenable
Trend Micro
Veritas
Western Digital
Xerox
Zebra

# --- block 2: case-insensitive (the lowercase is not a word) ---
@match insensitive
A10 Networks
Acronis
Advantech
Akamai
Alcatel
Almaviva
AMD
APC
Arcserve
Aruba
ASRock
ASUS
Atlassian
Autodesk
Avaya
Avira
Belden
Bitdefender
Broadcom
Citrix
Cloudera
Cloudflare
Commvault
CrowdStrike
D-Link
Dahua
Darktrace
Databricks
Datadog
Datalogic
Datto
Dedagroup
Dropbox
Dynatrace
Eaton
Epson
Ericsson
ESET
Extreme Networks
F5 Networks
Fastly
Forcepoint
Fortinet
Fujitsu
GPI
Grafana
Grandstream
Hikvision
HPE
Huawei
HubSpot
IBM
Imperva
JetBrains
Kaspersky
Kingston
Konica Minolta
Kyocera
Legrand
Lenovo
Lexmark
Maggioli
McAfee
MediaTek
Microsoft
MikroTik
Mimecast
Mitel
Nagios
Nakivo
NetApp
Netgear
Netskope
Nokia
Nutanix
NVIDIA
Paessler
Palo Alto Networks
Polycom
Proofpoint
Proxmox
QNAP
Qualcomm
Qualys
Rapid7
Ricoh
Riello
Rittal
Rubrik
Salesforce
Samsung
SanDisk
Schneider Electric
Seagate
SentinelOne
ServiceNow
SolarWinds
SonicWall
Sophos
Splunk
Supermicro
SUSE
Symantec
Synology
TeamSystem
Teradata
Toshiba
TP-Link
Trellix
Tripp Lite
Ubiquiti
Veeam
Vertiv
VMware
WatchGuard
Xiaomi
Yealink
Zabbix
Zendesk
Zimbra
Zoho
Zscaler
ZTE
Zucchetti
Zyxel
